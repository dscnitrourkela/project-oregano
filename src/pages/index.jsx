import React from 'react';
import { Navbar, Layout, Footer } from '../components';

const Homepage = () => (
  <div>
    <Navbar />
    <Layout />
    <Footer />
  </div>
);

export default Homepage;
