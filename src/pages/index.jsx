import React from 'react';
import { Navbar, Layout, Footer } from '../components';

const Homepage = () => (
  <div>
    Layout
    <Navbar />
    <Layout />
    <Footer />
  </div>
);

export default Homepage;
